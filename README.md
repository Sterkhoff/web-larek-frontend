# 3 курс
# Стерхов Максим Константинович
# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Webpack

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- src/pages/index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/index.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```
## Сборка

```
npm run build
```

или

```
yarn build
```
Описание архитектуры проекта 

# Описание базовых классов, их предназначение и функции.

Api - класс, описывающий работу с сервером
Функции:
get - Отправляет GET запрос по переданному URI и обрабатывает ответ сервера
get - Отправляет POST, PUT или DELETE запрос по переданному URI, в теле запроса передает переданный в нее объект
и обрабатывает ответ сервера

EventEmitter - класс, отвечающий за обработку событий
Функции:
on - Установить обработчик на событие;
emit - Инициировать событие с данными

# Описание паттерна MVC.
Model — отвечает за данные и бизнес-логику.
View — отображение данных, полученных от модели.
Controller — обрабатывает действия пользователя, отображает их во View.

# Model
BasketModel - отвечает за товары в корзине, обрабатывает их удаление и добавление.
OrderModel - класс, отвечающй за выбор способа оплаты, адреса доставки.
ProductGalleryModel - класс, отвечающий за ввод персональных данных покупателя.

# View
BasketItewmView - базовый класс для модальных окон
BasketView - отображение каталога товаров на главной странице
ContactsView - отображеие модального с описанием товара
ModalView - отображеие модального с информацией о товарах в корзине
OrderSuccessView - отображеие модального с информацией о способе оплаты и адресом доставки
OrderViewView - отображеие модального с контактами пользователя.
ProductCardFullView - отображеие модального окна успешного оформления заказа.
ProductCardGalleryView
ProductView

# Controller
BasketController - класс, отвечающий за логику добавления и удаления продуктов в корзину.
OrderController - класс, отвечающий за логику добавления деталей заказа.

# Описание интерфейсов в файле src/types/index.ts